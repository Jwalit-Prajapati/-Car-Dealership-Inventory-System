test(auth): add failing registration service and controller tests


Used an AI assistant to generate the initial failing TDD tests for registration request validation, auth service, and auth controller according to requirements.
